{
  "name": "charted",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-dev": "esbuild ./app_svg.js --bundle --sourcemap --outfile=./dist/bundle-dev.js",
    "build-watch": "esbuild ./src/svg.js --bundle --sourcemap --outfile=./dist/bundle-dev.js --format=umd --watch",
    "build-prod": "esbuild ./src/svg.js --bundle --minify --outfile=./dist/bundle-prod.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.0.4",
    "esbuild": "0.17.18"
  },
  "dependencies": {
    
  }
}
